@model IEnumerable<Employment_Project.Models.ViewModel.City>

@{
    ViewData["Title"] = "Index";
    int idno = 1;
}

<div class="container">

    <p class="text-center">
        <a class=" btn btn-primary " asp-action="AddorEdit"><i class="fa-solid fa-plus"></i> Create New</a>
    </p>

    <div class="text-center">


        <table class="table mx-auto text-center w-50 table-bordered">
            <thead>
                <tr>
                    <th>
                        Id
                    </th>
                    <th>
                        City Name
                    </th>
                    <th>
                        State
                    </th>
                    <th>
                        Action
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @idno
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.cityName)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem =>item.states.stateName)

                        </td>
                        <td class="text-end">

                            <a class=" btn btn-primary" href="City/AddorEdit/@item.id"><i class="fa-solid fa-pen-to-square"></i> Edit</a> |

                            <a class="delete-button btn btn-danger" href="City/Delete/@item.id" onclick="return confirm('Are you sure you want to delete this item?');"><i class="fa-solid fa-trash"></i> Delete</a>

                        </td>
                    </tr>

                    idno++; // Increment the idno after each iteration
                }

            </tbody>
        </table>
    </div>
</div>
